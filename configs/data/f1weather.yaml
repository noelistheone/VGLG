name: f1weather
root_path: data/f1weather
data_path: f1weather.csv
data_kind: custom         # 7:1:2 split on the time axis
features: M               # multivariate -> multivariate
target: AirTemp           # nominal target (only used in S / MS modes)
freq: t                   # 1-minute resolution within sessions
n_vars: 7                 # AirTemp, Humidity, Pressure, Rainfall, TrackTemp, WindDirection, WindSpeed
